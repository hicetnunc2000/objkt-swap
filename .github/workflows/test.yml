name: Unit Tests
on:
  pull_request:
    paths:
    - '.github/**'
    - '**.py'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.x'
          architecture: 'x64'
      - run: printf '%s\n' 'y' 'y' | sh <(curl -s https://smartpy.io/cli/install.sh)
      - run: pwd
      - run: ls -la
      - run: ls -la /home/runner/work/smart-contracts
      - run: ~/smartpy-cli/SmartPy.sh test smart-py/objkt_swap_tests.py /tmp/objkt_swap_test
      - run: ~/smartpy-cli/SmartPy.sh test smart-py/marketplace_tests.py /tmp/marketplace_test
  compile:
    needs: [test]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.x'
          architecture: 'x64'
      - run: printf '%s\n' 'y' 'y' | sh <(curl -s https://smartpy.io/cli/install.sh)
      - run: ~/smartpy-cli/SmartPy.sh compile smart-py/objkt_swap.py /tmp/objkt_swap
      - run: ~/smartpy-cli/SmartPy.sh compile smart-py/marketplace.py /tmp/marketplace
